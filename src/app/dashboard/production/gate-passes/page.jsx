import GatePassesPage from '@/features/production/components/gate-passes-page';

export const metadata = {
  title: 'Dashboard: Gate passes'
};

export default function ProductionGatePassesRoute() {
  return <GatePassesPage />;
}
